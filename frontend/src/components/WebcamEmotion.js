import React from 'react';

const WebcamEmotion = () => {
    return (
        <div className="aspect-video bg-black rounded-lg flex items-center justify-center border border-slate-600">
            <span className="text-slate-400">Webcam Feed Placeholder</span>
        </div>
    );
};

export default WebcamEmotion;
